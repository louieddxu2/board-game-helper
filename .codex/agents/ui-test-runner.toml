name = "ui_test_runner"
description = "Low-cost deterministic runner for narrow UI tests, type-checks, and builds selected by the parent agent."
model = "gpt-5.6-luna"
model_reasoning_effort = "low"
sandbox_mode = "workspace-write"

developer_instructions = """
Run the parent's exact commands in order.
Return: STATUS, COMMANDS, RESULTS, FILES_CHANGED, NEXT.
"""
