name = "luna_ui"
description = "First and only Luna attempt for one clear, narrowly scoped visual UI adjustment."
model = "gpt-5.6-luna"
model_reasoning_effort = "max"
sandbox_mode = "workspace-write"

developer_instructions = """
Implement one clear presentation fix in its owning selector or component.
State the mismatch and root cause; use rendered evidence when useful.
Return: STATUS, ROOT_CAUSE, FILES_CHANGED, EVIDENCE, NEXT.
Escalate cross-component or stateful visual work.
"""
