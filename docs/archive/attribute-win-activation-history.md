# 歷史紀錄：取勝方式合併的設計演進（非現行規格）

以下保留被取代的方案及當時狀態，僅供追溯。現行規格見 [取勝方式合併](../attribute-win-activation-plan.md)，請勿依本文件的舊操作步驟執行。

## 2026-09-09 修復

0091 最終修正：作者的五款指定分數修正原始匯入評分，不額外新增投票。移除 0090 多加的五筆作者修正記錄。正式執行與唯讀驗證分別使用 `node --import tsx scripts/replay-win-history.ts --apply`、`node --import tsx scripts/replay-win-history.ts --verify`；驗證會从原始事件與回答記錄從零重算，對照每款遊戲的分數、RD 與各項計數。

最新決策（0090）：不保留最終分數的彙總種子。逐筆轉換兩個舊屬性的投票與顯示內容，得分端反向、條件端原向；刪除 0089 的彙總事件。作者指定的五款修正另存為普通直接評分。所有新屬性分數均由逐筆記錄重播產生，可能不同於 0088 的最終分數平均。0090 建立只重播取勝方式的工作；部署後執行 `npx tsx scripts/replay-win-history.ts --apply` 並確認工作 completed。之後完整重建仍只依投票記錄，不再對取勝方式使用特殊 RD 或已吸收標記。

0083～0088 已發布。0089 將 generation 88 中的轉換值保存為可重播的 rating event，沿用一筆轉換資料與 RD=3 的政策，確保遊戲合併清空分數表後仍可還原。新票繼續按時間順序重播，不重複計算。0089 要求原始 generation 88 快照存在，缺失時中止，須從備份恢復快照後才能套用。舊屬性歷史仍不參與計分。

目錄增量刪除直接使用完整 value key，支援含冒號的 subject ID；刪除屬性時也清除其值。分頁同步期間保留先於屬性定義抵達的分數，避免誤刪新屬性的資料。

狀態：一次性轉換 migration 0088 已完成程式實作與隔離資料庫驗證，尚未執行遠端 migration。依據 2026-09-08 預覽（公開目錄版本 8109）與目前程式碼。

## 2026-09-08 實作進度：一次性轉換

- 新增 migration 0085，僅建立獨立批次與初始值來源表，不新增屬性、不填分數、不啟用合併。來源 JSON、公式版本及切換時間可持久保存。
- 計分狀態新增可選 `initialScore`。零票初始值遇到第一筆直接分數時使用既有模型更新，真實票數只增加一次；無初始值的舊屬性維持首次直接評分行為。
- 實際回應讀取以主鍵連接初始值表，不新增全表查詢。重建初始化從獨立表還原零票分數，再重播切換邊界之後的答案。
- 純重播函式支援初始值、subject 映射及 responseId 去重；相同 responseId 內容衝突、映射後初始值衝突均拒絕。
- 隔離 SQLite 套用全部 migrations，驗證實際寫入、重送、背景重建與歷史邊界過濾，來源快照不變。
- migration 0088 建立正式雙極屬性「取勝方式」，固定 0＝得分取勝、10＝條件取勝，並複製兩端原文。
- 以目前兩個來源的有效 materialized score 一次換算：兩端用 `((10 − 得分取勝) + 條件取勝) / 2`，缺一端換向後使用，兩端皆無資料保留未評分；五款指定修正寫入轉換結果。
- 合併結果直接寫入新屬性作為一筆轉換資料；舊屬性停用、舊 materialized states 與 pair stats 移除，舊 response/event 原始列只作封存，不再參與出題或重建。
- 候選資料的兩個來源欄位合併成一欄，目錄 snapshot 重新建立，近期活動快照清除後由新屬性重新累積。
- migration 0085～0087 的保留／映射結構仍可留在資料庫，但本次正式切換不填舊題 mapping，也不依賴初始值 baseline。
- 尚未執行遠端 migration。部署新版讀取程式前，須先套用 0083～0088。

## 保持不變

- 固定 0＝得分取勝、10＝條件取勝。投票題可反向，儲存與瀏覽固定方向。
- 一次性轉換使用已同意的平均公式；五款校正只套用在合併結果，不修改舊投票。快餐兩筆來源不校正。
- 舊 response/event 原始列保留作封存，但不再被視為可投票屬性；正式公開資料只使用新屬性。
- 正式部署、遠端 migration 與 push 不在本次工作範圍。

## 已確認問題（已由一次性轉換取代）

以下是原本「保留舊資料並以初始值延續」方案的問題紀錄；正式啟用改採一次性轉換後，0088 不再依賴這條路徑。

1. `worker/data/attributeScoring.ts` 的 `applyDirectRating` 在 evidenceCount＝0 時把分數直接改成首次輸入值。若只寫入平均初始值而保持零票，首次投票會完全抹去初始值。
2. `worker/data/attributes.ts` 的 `initializeAttributeMergeRebuild` 會清空全部狀態，從 5 分重建。沒有獨立初始值來源時，遊戲合併或背景重建會遺失換算結果。
3. `responseContextAndStates` 目前要求屬性 is_active＝1。直接停用兩個舊屬性會令仍在離線佇列的合法舊題失敗。
4. 目前範例抽取與相近遊戲依 evidenceCount 判定有無資料。若新屬性沒有任何新票，換算初始值會被當成未知；填一張假票雖能繞過判斷，但不符合不增加投票的要求。

## 舊方案記錄（已不作為本次啟用前提）

### A. 獨立初始值與來源

- 新增獨立的合併批次與每款初始值紀錄，保存來源兩個 attribute ID、subject ID、完整來源狀態、版本、時間、公式版本與五款校正。
- 平均分數與投票統計分開。合併初始時 directCount、comparisonCount、evidenceCount 都為 0；另有明確的初始值存在標記，未知列沒有初始值。
- RD 採保守初始不確定度（現有模型上限 3），不以來源票數推算縮小後的 RD。這是初始化政策，不是對舊票信心的精確合成。
- 計分器辨識初始值存在時，不走「第一票直接覆蓋」分支；後續真實投票按既有更新公式計算，只新增真實票數。
- 有初始值但無新票的瀏覽文字標示「由舊屬性換算／新投票 0」，不可顯示「已獲 1 票」。範例可使用初始值；相近排序須保留保守權重，不能把兩個來源重複當成兩個維度。

### B. 重建與新資料

- 重建先載入合併初始值，再只重播切換之後屬於新屬性的有效答案；不得把初始值已吸收的舊歷史再計一次。
- 合併遊戲時，初始值來源 subject 的對應也需更新或保留映射。來源與目標都有初始值時，必須有明確、可重建的合併規則；不能依資料列先後決定。
- 分開「可抽題」與「可接受有效舊簽章回答」。切換後停止抽舊題，但仍驗證原始題目的屬性、兩款遊戲、方向及有效期限，再處理舊答案。
- 舊得分取勝答案映射到固定方向時用 10−分數，A_HIGHER／B_HIGHER 對調；舊條件取勝保持原方向。SIMILAR 保持不變。新的雙端答案沿用既有 highPole 換向。
- 同一 responseId 只能計一次；儲存來源題目識別與映射版本，使重送及背景重播不重複換向。
- 舊答案不先改動已凍結的初始值再投一次新票；只能透過一條明確的映射路徑納入後續更新。
- 新題只從新雙極屬性抽取；停用來源屬性的舊簽章回答不再接受，避免在一次性轉換後產生第二套資料流。
- 遊戲合併只重建目前 active 屬性的資料，不重播封存的來源屬性 response/event。

### C. 原子切換與回復

- 啟用時重抓最新正式狀態，對照預覽及五款精確 ID；若來源缺失、版本不同或已有同名批次，先停止並產出差異。
- 以受控批次建立新屬性與兩端原文、來源快照及初始狀態，再切換抽題／公開目錄；在此之前保留現有服務。
- 同一批次的重跑要冪等。切換、背景重建及投票写入不能交錯到不同初始版本。
- 快照與增量一併發布，確保舊快取不繼續把新舊三個欄位都當成獨立可投票維度。
- 回復優先停用新抽題、恢復舊欄位曝光；不刪除任何新投票、映射收據或來源快照。回復後新票如何呈現另行處理，不把單一新值虛構為兩筆舊答案。

## 必須通過的驗收

- 新屬性有資料時，第一筆新投票沿既有模型更新；轉換資料不再重建成舊屬性。
- 未知項目維持 5 分／0 證據，等待新投票。
- 五款校正、候選欄位與目錄 snapshot 均可由轉換結果驗證，沒有雙重計票。
- 重跑 migration、背景重建及合併遊戲後只使用 active 屬性，封存來源不再回流。
- 新雙極題的兩個 highPole 方向、分數換向與比較結果交換仍須通過既有簽章驗收。
- 桌面／手機瀏覽、總表排序、兩端範例、直接評分歷史、相近遊戲與收藏抽題都辨識合併屬性。
- 舊 raw response/event 保留作查核，但不再出題、顯示活動或參與合併重建。

一次性轉換 migration 0088 已涵蓋本次必要驗收；以上舊方案不再作為啟用前提。
